language: generic
sudo: required
services:
    - docker

before_install:
    - docker build -t rwuniard/react-test -f ./client/Dockerfile.dev ./client

script:
    - docker run -e CI=true rwuniard/react-test npm test

after_success:
    - docker build -t rwuniard/multi-client ./client
    - docker build -t rwuniard/multi-nginx ./nginx
    - docker build -t rwuniard/multi-server ./server
    - docker build -t rwuniard/multi-worker ./worker

    # Login to docker hub using docker CLI
    # passing the password to the docker login std in
    - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
    # Pushing images to Docker hub
    - docker push rwuniard/multi-client
    - docker push rwuniard/multi-nginx
    - docker push rwuniard/multi-server
    - docker push rwuniard/multi-worker
